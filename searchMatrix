bool searchMatrix(vector<vector<int>>& mat, int target) {
        for(auto x: mat){
            for(auto y:x){
                if(y == target){
                    return true;
                }
            }
        }
        return false;
}
